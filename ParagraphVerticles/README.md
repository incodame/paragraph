This is a data collector application, offering a query interface via https. 
It should allow a distributed form of datalog, and remain secure by only
 allowing queries over facts concerning deployed applications, not remote
 system infrastructure.

